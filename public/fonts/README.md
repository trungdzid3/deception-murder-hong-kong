Place the custom title font `KK7-Dia-Dao.ttf` in this folder so the site can load it via `/fonts/KK7-Dia-Dao.ttf`.

Instructions:
1. Copy the provided `KK7-Dia-Dao.ttf` file into `public/fonts/`.
2. Restart your dev server if it's running (Vite caches assets).

If you prefer to embed the font differently, update the `@font-face` src in `src/index.css` accordingly.